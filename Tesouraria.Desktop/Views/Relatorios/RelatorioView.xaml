<UserControl x:Class="Tesouraria.Desktop.Views.Relatorios.RelatorioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White"
             HorizontalAlignment="Center" 
             VerticalAlignment="Center">

    <Border Background="White" CornerRadius="5" Padding="30" MinWidth="400" MinHeight="300">
        <Grid Margin="30">
            <StackPanel MaxWidth="600" HorizontalAlignment="Left">
                <TextBlock Text="Relatórios Gerenciais" FontSize="24" FontWeight="Bold" Foreground="#2980B9" Margin="0,0,0,20"/>

                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Filtros do Relatório" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Data Início"/>
                                <DatePicker SelectedDate="{Binding DataInicio}" Height="35"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Data Fim"/>
                                <DatePicker SelectedDate="{Binding DataFim}" Height="35"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Centro de Custo (Opcional)"/>
                        <ComboBox ItemsSource="{Binding CentrosCusto}" 
                                  DisplayMemberPath="Nome" 
                                  SelectedValuePath="Id"
                                  SelectedValue="{Binding CentroCustoId}"
                                  Height="35" Margin="0,0,0,15"/>

                        <CheckBox Content="Filtrar pela Data do Pagamento (Regime de Caixa)" 
                                  IsChecked="{Binding FiltrarPorPagamento}"
                                  Margin="0,0,0,20"/>

                        <Button Content="🖨️ Gerar Relatório em PDF" 
                                Command="{Binding GerarCommand}"
                                Height="45" Background="#2980B9" Foreground="White" 
                                FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Border>

</UserControl>