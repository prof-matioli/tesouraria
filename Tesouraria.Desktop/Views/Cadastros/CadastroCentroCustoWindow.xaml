<Window x:Class="Tesouraria.Desktop.Views.Cadastros.CadastroCentroCustoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tesouraria.Desktop.Views.Cadastros"
        mc:Ignorable="d"
        Title="Centros de Custo" Height="500" Width="800"
        WindowStartupLocation="CenterScreen" Background="#F4F6F7">

    <Window.Resources>
        <Style TargetType="Button" x:Key="BaseButtonStyle">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20" Orientation="Horizontal">
            <TextBlock Text="🏢" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <StackPanel>
                <TextBlock Text="Centros de Custo" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Gerencie os Centros de Custos" FontSize="14" Foreground="#7F8C8D"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="5">
            <DataGrid ItemsSource="{Binding Items}" 
                      SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" IsReadOnly="True"
                      RowHeight="35" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="2*"/>
                    <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="3*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="🔄 Atualizar" 
                    Command="{Binding BuscarCommand}"
                    Style="{StaticResource BaseButtonStyle}"
                    Background="#95A5A6" Foreground="White" Width="120"/>

            <Button Content="🗑️ Excluir" 
                    Command="{Binding ExcluirCommand}"
                    Style="{StaticResource BaseButtonStyle}"
                    Background="#E74C3C" Foreground="White" Width="120"/>

            <Button Content="✏️ Editar" 
                    Command="{Binding EditarCommand}"
                    Style="{StaticResource BaseButtonStyle}"
                    Background="#3498DB" Foreground="White" Width="120"/>

            <Button Content="➕ Novo" 
                    Command="{Binding NovoCommand}"
                    Style="{StaticResource BaseButtonStyle}"
                    Background="#2ECC71" Foreground="White"
                    Padding="20,0" Width="120"/>

        </StackPanel>
    </Grid>
</Window>