<UserControl x:Class="Tesouraria.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:vm="clr-namespace:Tesouraria.Desktop.ViewModels"
             
             mc:Ignorable="d" d:DesignWidth="900"
             Background="#F5F7FA" Height="618">

    <UserControl.Resources>
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E0E0E0" BlurRadius="15" ShadowDepth="2" Direction="270" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="CardTitleStyle">
            <Setter Property="Foreground" Value="#757575"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="CardValueStyle">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Visão Geral" FontSize="24" FontWeight="Bold" Foreground="#333"/>
            <TextBlock Text="Acompanhe o fluxo financeiro da paróquia." FontSize="14" Foreground="#888" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="RECEITAS (MÊS)" Style="{StaticResource CardTitleStyle}"/>
                        <TextBlock Text="{Binding Resumo.TotalReceitas, StringFormat={}{0:C2}}" 
                                   Style="{StaticResource CardValueStyle}" 
                                   Foreground="#2E7D32"/>
                        <TextBlock Text="{Binding Resumo.ComparativoReceita}" 
                                   FontSize="11" Foreground="#888" Margin="0,5,0,0"/>
                    </StackPanel>

                    <Border Grid.Column="1" Width="40" Height="40" CornerRadius="20" Background="#E8F5E9" VerticalAlignment="Top">
                        <TextBlock Text="↑" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#2E7D32" FontWeight="Bold" FontSize="18"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="DESPESAS (MÊS)" Style="{StaticResource CardTitleStyle}"/>
                        <TextBlock Text="{Binding Resumo.TotalDespesas, StringFormat={}{0:C2}}" 
                                   Style="{StaticResource CardValueStyle}" 
                                   Foreground="#C62828"/>
                        <TextBlock Text="{Binding Resumo.ComparativoDespesa}" 
                                   FontSize="11" Foreground="#888" Margin="0,5,0,0"/>
                    </StackPanel>

                    <Border Grid.Column="1" Width="40" Height="40" CornerRadius="20" Background="#FFEBEE" VerticalAlignment="Top">
                        <TextBlock Text="↓" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#C62828" FontWeight="Bold" FontSize="18"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="0">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="STATUS DO CAIXA" Style="{StaticResource CardTitleStyle}"/>
                        <TextBlock Text="Operacional" Style="{StaticResource CardValueStyle}" Foreground="#1565C0"/>
                        <TextBlock Text="Dados atualizados em tempo real" FontSize="11" Foreground="#888" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="15" ShadowDepth="2" Direction="270" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Histórico Financeiro (Últimos 6 Meses)" 
                           FontSize="16" FontWeight="SemiBold" Foreground="#444" Margin="0,0,0,15"/>

                <lvc:CartesianChart Grid.Row="1" 
                                    Series="{Binding SeriesGrafico}" 
                                    LegendLocation="Bottom">

                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Período" 
                                  Labels="{Binding LabelsGrafico}" 
                                  Foreground="#666">
                            <lvc:Axis.Separator>
                                <lvc:Separator Step="1" StrokeThickness="0" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>

                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Valor (R$)" 
                                  LabelFormatter="{Binding YFormatter}" 
                                  Foreground="#666">
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="#EEE" StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>

                </lvc:CartesianChart>
            </Grid>
        </Border>
    </Grid>
</UserControl>